# Desktop Hello World Intent

This bundle defines baseline intent for the DawnChat desktop hello world template.

- Frontend source is under `_ir/frontend/web-src`.
- Backend entry is implemented with Bun under `_ir/backend/entry/main.ts`.
- Home page stays intentionally simple with one greeting card.
- Primary backend API is `GET /api/hello?name=...`.
